// TODO: Scout — explora portales nuevos con Playwright.
// Plan:
//   - Recibir { portal, url } como input.
//   - Lanzar browser stealth (reusar koneshBrowser pattern del Reino A si aplica).
//   - Capturar selectores de inputs/botones, formularios, captchas, requests críticos.
//   - Detectar stack (Angular/React/JSP), endpoints WS/REST relevantes, cookies de sesión.
//   - Devolver mapa estructural (JSON) consumible por Generator.

async function explorarPortal() {
  throw new Error('scout.explorarPortal — TODO');
}

module.exports = { explorarPortal };
